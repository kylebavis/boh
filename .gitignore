bin/
obj/
*.user
*.suo
.vs/
.vscode/
.idea/

# Local runtime data (SQLite DB, blobs, thumbnails). Anchored to the repository root:
# unanchored, it also matches src/Boh.Web/Data/ and silently ignores new source files there.
/data/
*.db
*.db-wal
*.db-shm

# Test output
TestResults/
*.trx
coverage*.xml

# Local tool settings, specific to one machine's permission grants
.claude/settings.local.json
